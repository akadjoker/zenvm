struct Vec3 { x, y, z }

def dot(a, b) {
    return a.x * b.x + a.y * b.y + a.z * b.z;
}

var v1 = Vec3(1.1, 2.2, 3.3);
var v2 = Vec3(4.4, 5.5, 6.6);
var acc = 0.0;
var n = 1000000;
var i = 0;
while (i < n) {
    acc = acc + dot(v1, v2);
    i = i + 1;
}
print(acc);
