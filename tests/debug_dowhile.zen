var outer = 0;
do {
    outer = outer + 1;
    var inner = 0;
    do {
        inner = inner + 1;
        if (inner == 4) { break; }
    } while (inner < 10);
    print(outer, inner);
} while (outer < 3);
